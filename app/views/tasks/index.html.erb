<h1>My Tasks</h1>

<% @tasks.each do |task| %>

  <% if task.completed %>
    <input type="checkbox" name="task" checked>
  <% else %>
    <input type="checkbox" name="task">
  <% end %>

  <label for="task"><%= task.title %></label>
  <p><%= task.details %></p>
<% end %>
